#!/bin/bash

SOURCE="$( dirname "${BASH_SOURCE[0]}" )"

if [ -z "$1" ]; then
  echo "Error: No filename provided."
  exit 1
fi

if [ ! -s ./$1.cpp ]; then
  touch ./$1.cpp
  cat $SOURCE/template.cpp > ./$1.cpp
else
  echo "$1.cpp exists!!"
fi
touch ./$1.inp
touch ./$1.sout
