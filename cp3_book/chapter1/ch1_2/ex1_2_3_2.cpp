#define _USE_MATH_DEFINES

#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    char output[50];
    int n;
    cin >> n;
    cout << setprecision(n + 1) << M_PI;
    return 0;
}